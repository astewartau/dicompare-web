{
  "id": "uniform_echo_spacing",
  "name": "Uniform Echo Spacing",
  "description": "Validates that echo times are evenly spaced, within a configurable tolerance.",
  "category": "Echo Timing",
  "fields": [
    "EchoTime"
  ],
  "requiredSystemFields": [],
  "implementation": "te = value[\"EchoTime\"].dropna().sort_values()\nspacings = te.diff().iloc[1:]\ntol = params.get(\"tolerance_ms\") or 0.1\nif len(spacings) > 0 and not all(abs(spacings.iloc[0] - s) <= tol for s in spacings):\n    raise ValidationError(f\"Echo spacing not uniform (tolerance {tol} ms). Found spacings: {list(spacings.round(3))}.\")\n",
  "testCases": [
    {
      "id": "uniform_pass",
      "name": "Evenly spaced echoes pass",
      "expectedResult": "pass",
      "data": {
        "EchoTime": [
          5.0,
          10.0,
          15.0,
          20.0
        ]
      }
    },
    {
      "id": "nonuniform_fail",
      "name": "Uneven spacing fails",
      "expectedResult": "fail",
      "data": {
        "EchoTime": [
          5.0,
          10.0,
          17.0
        ]
      }
    },
    {
      "id": "tolerance_moves",
      "name": "Uneven spacing passes with loose tolerance",
      "expectedResult": "pass",
      "data": {
        "EchoTime": [
          5.0,
          10.0,
          17.0
        ]
      },
      "params": {
        "tolerance_ms": 5.0
      }
    }
  ],
  "parameterDefinitions": [
    {
      "name": "tolerance_ms",
      "label": "Tolerance",
      "type": "number",
      "default": 0.1,
      "min": 0,
      "unit": "ms",
      "description": "Maximum allowed deviation between echo spacings"
    }
  ]
}
