{
  "id": "t1w_isotropic_1mm",
  "name": "~1 mm Isotropic",
  "description": "Use ~1 mm isotropic voxels, whole head. (From the HC-ChiSep Validation Protocol schema.)",
  "category": "Geometry",
  "fields": [
    "PixelSpacing",
    "SliceThickness"
  ],
  "requiredSystemFields": [],
  "parameters": null,
  "implementation": "ps = value[\"PixelSpacing\"].iloc[0]\nst = value[\"SliceThickness\"].iloc[0]\nif abs(ps[0]-ps[1]) > 0.1 or abs(ps[0]-st) > 0.15:\n    raise ValidationError(f\"Voxels not isotropic. PixelSpacing {ps}, SliceThickness {st}.\")\nif max(ps[0], ps[1], st) > 1.2:\n    raise ValidationWarning(f\"Voxel edge {max(ps[0],ps[1],st)} mm > ~1 mm.\")",
  "testCases": [
    {
      "id": "tc_Pass",
      "name": "Pass",
      "data": {
        "PixelSpacing": [
          [
            1,
            1
          ]
        ],
        "SliceThickness": [
          1
        ]
      },
      "expectedResult": "pass",
      "description": ""
    },
    {
      "id": "tc_Fail_aniso",
      "name": "Fail_aniso",
      "data": {
        "PixelSpacing": [
          [
            1,
            1
          ]
        ],
        "SliceThickness": [
          1.5
        ]
      },
      "expectedResult": "fail",
      "description": ""
    }
  ]
}
