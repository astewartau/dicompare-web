{
  "id": "low_bvalue_present",
  "name": "Low b-value Present",
  "description": "Validates that each entry includes at least one non-zero b-value below a configurable threshold (e.g. for accurate noise mapping).",
  "category": "Diffusion",
  "fields": [
    "DiffusionBValues"
  ],
  "requiredSystemFields": [],
  "implementation": "thr = params.get(\"max_low_b\") if params.get(\"max_low_b\") is not None else 1100\nerrors = []\nfor i in range(len(value[\"DiffusionBValues\"])):\n    bvals = as_list(value[\"DiffusionBValues\"][i])\n    if len([x for x in bvals if 0 < x < thr]) == 0:\n        errors.append(f\"Entry {i}: no non-zero b-value below {thr:g} s/mm\u00b2 present\")\nif errors:\n    raise ValidationError(chr(10).join(errors))\n",
  "testCases": [
    {
      "id": "low_b_pass",
      "name": "Low b-value present",
      "expectedResult": "pass",
      "data": {
        "DiffusionBValues": [
          [
            0,
            1000,
            3000
          ]
        ]
      }
    },
    {
      "id": "no_low_b_fail",
      "name": "Only high b-values fail",
      "expectedResult": "fail",
      "data": {
        "DiffusionBValues": [
          [
            0,
            3000,
            5000
          ]
        ]
      }
    },
    {
      "id": "threshold_moves",
      "name": "b=1000 fails when threshold lowered to 500",
      "expectedResult": "fail",
      "data": {
        "DiffusionBValues": [
          [
            0,
            1000,
            3000
          ]
        ]
      },
      "params": {
        "max_low_b": 500
      }
    }
  ],
  "parameterDefinitions": [
    {
      "name": "max_low_b",
      "label": "Low b-value threshold",
      "type": "number",
      "default": 1100,
      "min": 0,
      "unit": "s/mm\u00b2",
      "description": "At least one non-zero b-value below this must be present"
    }
  ]
}
